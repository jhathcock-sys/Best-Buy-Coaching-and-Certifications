# Agent Memory

This directory contains the single source of truth for all actions and tasks completed by the AI agents in this project. 

Logs are divided into daily files (`YYYY-MM-DD.md`). The AI is instructed via `.agents/AGENTS.md` to automatically append its progress and completed tasks to these files.

This creates a persistent memory across different chat sessions and helps track the evolution of the project.
