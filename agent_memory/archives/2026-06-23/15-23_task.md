# Post-MVP Enterprise Overhaul Tasks

- `[x]` **Authentication & Security**
  - `[x]` **Phase 1: Enterprise Authentication & Firestore Migration**
  - `[x]` Map `users` collection to Firestore; remove `sessionStorage` fallback and `storePin` logic from `authSlice.ts`.
  - `[x]` Update `AdvisorLogin.tsx` to authenticate against the cloud `users` collection.
  - `[x]` Clear Zustand slices (`rosterHistory`, `playbookSettings`) completely on `logout()`.
  - `[x]` Implement `firestore.rules` for strict Role-Based Access Control (RBAC).

- `[x]` **AI Integration & Stability**
  - `[x]` Update `generateCoachingLogGemini` and simulator APIs to use `responseSchema` for guaranteed JSON.
  - `[x]` Add exponential backoff retry logic to `src/services/ai.ts`.
  - `[x]` Enforce `PlaybookSettings` globally across all AI generators.

- `[x]` **UX Purge & React 19 Architecture**
  - `[x]` Purge all inline `style={{...}}` blocks and replace with Tailwind/CSS variables. (Adhering to incremental Boy Scout rule)
  - `[x]` Inject `:hover` micro-animations and deep glassmorphism across modals.
  - `[x]` Implement `useShallow` on massive Zustand selectors to prevent re-render cascades.
  - `[x]` Enable the React 19 Compiler in `vite.config.ts`.
  - `[x]` Sanitize all AI markdown outputs using `DOMPurify` to prevent XSS.

- `[x]` **BI Analytics & Validation**
  - `[x]` Refactor `MetricAverages` to track `totalRevenue` and `totalHours`.
  - `[x]` Update store-wide RPH and Warranty calculations to use weighted math.
  - `[x]` Run `npm run lint` and `npx tsc --noEmit` to verify type safety.
